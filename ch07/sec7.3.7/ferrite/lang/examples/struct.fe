;; Struct example with pattern matching
(defstruct Point (x i32) (y i32))

(defn main ()
  (let ((p (Point 3 4)))
    (match p
      ((Point x y) 
        (let ((sum (+ x y)))
          (print sum))))))
