
## block.py

A minimal blockchain in ~50 lines of pure Python (no external dependencies).
See `README.md` for a full explanation of the design and how to run it.

Prerequisites: Python 3.x, standard library only (`hashlib`, `time`).

```bash
python3 block.py
```
